/*
package ir

object BuildSTListener extends STListener[Unit] {

  def initState: Unit = ()

  override def enter(node: Ir): S => S = identity[S]

  override def leave(node: Ir): S => S = identity[S]
}
*/
